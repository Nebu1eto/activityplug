pre-commit:
  # Tarball tests run prepack builds against the same dist directories.
  parallel: false
  commands:
    typecheck:
      run: pnpm typecheck
    format:
      run: pnpm format:check
    lint:
      run: pnpm lint
    test:
      run: pnpm test
