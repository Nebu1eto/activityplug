{
  "name": "@activityplug/example-web-client",
  "version": "0.0.0",
  "private": true,
  "description": "Sample web client for ActivityPlug.",
  "license": "Apache-2.0 OR MIT",
  "type": "module",
  "scripts": {
    "build": "tsdown src/index.ts --format esm --dts --clean"
  },
  "dependencies": {
    "@activityplug/core": "workspace:*"
  }
}
