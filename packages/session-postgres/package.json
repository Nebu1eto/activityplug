{
  "name": "@activityplug/session-postgres",
  "version": "0.0.0",
  "description": "PostgreSQL auth session storage for ActivityPlug server mode.",
  "license": "Apache-2.0 OR MIT",
  "files": [
    "dist"
  ],
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.mts",
      "default": "./dist/index.mjs"
    }
  },
  "scripts": {
    "build": "tsdown src/index.ts --format esm --dts --clean"
  },
  "dependencies": {
    "@activityplug/server": "workspace:*"
  }
}
