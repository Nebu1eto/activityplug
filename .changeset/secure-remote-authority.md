---
"@activityplug/core": major
---

Require an explicit `RemoteAuthority` for direct clients, remove
credential-bearing stream URLs, add bounded operation budgets, and expose
portable credential leases.
